package simpleMoney.library;

public enum ResponseCode {
    SUCCESS,
    DUPLICATE_ACCOUNT,
    BAD_REQUEST,
    FAILURE,
    PARSER_ERROR,
    INVALID_ID,
    NOT_FOUND,
    INSUFFICIENT_BALANCE;
}
